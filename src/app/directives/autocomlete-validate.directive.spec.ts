import { AutocomleteValidateDirective } from './autocomlete-validate.directive';

describe('AutocomleteValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocomleteValidateDirective();
    expect(directive).toBeTruthy();
  });
});
